# Supabase (projeto carpet-acai — tabelas move_ isoladas)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# A senha do admin NÃO fica aqui — ela é validada no banco (move_settings.admin_secret).
# Senha inicial: movemais2026  (troque no painel: aba não implementada? use SQL move_admin_set_secret)
